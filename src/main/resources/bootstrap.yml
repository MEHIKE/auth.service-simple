server:
  port: 8085

spring:
  application:
    name: auth_service_simple
  cloud:
    config:
      enabled: true
      #uri: http://config:8088
      uri: http://${CONFIG_SERVICE_HOST}:${CONFIG_SERVICE_PORT}
      